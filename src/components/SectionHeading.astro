---
import type { SectionHeadingProps as Props } from "@/types";

const { class: className = "" } = Astro.props;
---

<div
	class={`mb-16 flex flex-col items-center text-center justify-center gap-y-6 max-w-3xl mx-auto text-balance ${className}`.trim()}
>
	<slot />
</div>
