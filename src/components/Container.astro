---
import { twMerge } from "tailwind-merge";
import type { ContainerProps as Props } from "@/types";
const { class: className = "", contain = true } = Astro.props;
const classes = twMerge(contain ? "container py-16 lg:py-24" : "", className);
---

<div class={classes}>
	<slot />
</div>
