---
import Layout from "@/layouts/Layout.astro";
import Section from "@/components/Section.astro";
import Container from "@/components/Container.astro";
import SectionHeading from "@/components/SectionHeading.astro";
import FormContact from "@/components/forms/FormContact.astro";
import FormEvent from "@/components/forms/FormEvent.astro";
import { phone, email, address } from "@/data/config";

// Page metadata
const title = "Contact";
const description = "Hamaynk contact page for Armenian events in New England";
const seoTitle = "Contact";
const seoDescription = "Hamaynk contact page for Armenian events in New England";
---

<Layout {title} {description} {seoTitle} {seoDescription}>
	<Section>
		<Container>
			<SectionHeading>
				<h1 class="text-4xl font-bold tracking-wide font-caveat-brush">Contact Us 📝</h1>
				<p>Have questions or comments to share with us? We’d love to hear from you!</p>
			</SectionHeading>

			<div class="grid grid-cols-1 lg:grid-cols-5 gap-8">
				<!-- Sidebar -->
				<div class="lg:col-span-2 flex flex-col gap-6 lg:justify-between">
					<!-- Get In Touch -->
					<div class="rounded-2xl grow bg-white border border-gray-200 p-6 flex flex-col gap-4">
						<h2 class="font-caveat-brush text-xl font-semibold">Get In Touch</h2>
						<div class="flex items-start gap-3">
							<svg
								xmlns="http://www.w3.org/2000/svg"
								class="size-5 text-brand-blue mt-0.5 shrink-0"
								fill="none"
								viewBox="0 0 24 24"
								stroke-width="1.5"
								stroke="currentColor"
							>
								<path
									stroke-linecap="round"
									stroke-linejoin="round"
									d="M21.75 6.75v10.5a2.25 2.25 0 0 1-2.25 2.25h-15a2.25 2.25 0 0 1-2.25-2.25V6.75m19.5 0A2.25 2.25 0 0 0 19.5 4.5h-15a2.25 2.25 0 0 0-2.25 2.25m19.5 0v.243a2.25 2.25 0 0 1-1.07 1.916l-7.5 4.615a2.25 2.25 0 0 1-2.36 0L3.32 8.91a2.25 2.25 0 0 1-1.07-1.916V6.75"
								></path>
							</svg>
							<a href={email.href} class="text-sm text-gray-500 hover:text-brand-blue transition">
								{email.label}
							</a>
						</div>
						<div class="flex items-start gap-3">
							<svg
								xmlns="http://www.w3.org/2000/svg"
								class="size-5 text-brand-blue mt-0.5 shrink-0"
								fill="none"
								viewBox="0 0 24 24"
								stroke-width="1.5"
								stroke="currentColor"
							>
								<path
									stroke-linecap="round"
									stroke-linejoin="round"
									d="M12 6v6h4.5m4.5 0a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z"></path>
							</svg>
							<span class="text-sm text-gray-500">Open 24/7</span>
						</div>
					</div>

					<!-- Enrollment Info -->
					<div class="rounded-2xl grow border bg-white border border-gray-200 p-6 flex flex-col gap-4">
						<div class="flex items-center gap-3">
							<svg
								xmlns="http://www.w3.org/2000/svg"
								class="size-6 text-brand-blue shrink-0"
								fill="none"
								viewBox="0 0 24 24"
								stroke-width="1.5"
								stroke="currentColor"
							>
								<path
									stroke-linecap="round"
									stroke-linejoin="round"
									d="M19.5 14.25v-2.625a3.375 3.375 0 0 0-3.375-3.375h-1.5A1.125 1.125 0 0 1 13.5 7.125v-1.5a3.375 3.375 0 0 0-3.375-3.375H8.25m0 12.75h7.5m-7.5 3H12M10.5 2.25H5.625c-.621 0-1.125.504-1.125 1.125v17.25c0 .621.504 1.125 1.125 1.125h12.75c.621 0 1.125-.504 1.125-1.125V11.25a9 9 0 0 0-9-9Z"
								></path>
							</svg>
							<h3 class="font-caveat-brush text-lg font-semibold">Disclosures</h3>
						</div>
						<ul class="text-sm text-gray-500 flex flex-col gap-2 list-disc list-inside">
							<li>Your event may take up to a week to be published, so submit in advance!</li>
							<li>Double check all details as our forms are NOT editable!</li>
							<li>Note that submitted events will be reviewed for approval.</li>
							<li>There must be a valid direct link (URL) to your event in order to be considered. </li>
							<li>We reserve the right to accept or decline events at our own discretion.</li>
						</ul>
					</div>
				</div>

				<!-- Form -->
				<div class="lg:col-span-3">
					<div class="bg-white rounded-2xl border border-gray-200 p-6 flex flex-col gap-6">
							<FormEvent />
					</div>
				</div>
			</div>
		</Container>
	</Section>
</Layout>

<script>
	const btns = document.querySelectorAll("[data-tab-btn]") as NodeListOf<HTMLButtonElement>;
	const panels = document.querySelectorAll("[data-tab-panel]") as NodeListOf<HTMLElement>;
	const title = document.querySelector("[data-tab-title]") as HTMLElement | null;

	const titles: Record<string, string> = {
		contact: "Send Us a Message",
		event: "Event Submission",
	};

	const activeClasses = ["bg-brand-blue", "text-white", "hover:bg-brand-blue-dark"];
	const inactiveClasses = ["border", "border-gray-300", "text-gray-800", "hover:bg-gray-100"];

	function activate(tab: string) {
		btns.forEach((btn) => {
			const isActive = btn.dataset.tabBtn === tab;
			if (isActive) {
				btn.classList.remove(...inactiveClasses);
				btn.classList.add(...activeClasses);
			} else {
				btn.classList.remove(...activeClasses);
				btn.classList.add(...inactiveClasses);
			}
		});

		panels.forEach((panel) => {
			panel.hidden = panel.dataset.tabPanel !== tab;
		});

		if (title) title.textContent = titles[tab] ?? "";
	}

	btns.forEach((btn) => btn.addEventListener("click", () => activate(btn.dataset.tabBtn!)));
</script>
